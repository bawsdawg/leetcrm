import { WorkspacePlaceholder } from "@/components/crm/workspace-placeholder";

export const metadata = { title: "Brugerstyring · 1337-crm by Searchmind" };

export default function UsersAdminPage() {
  return (
    <WorkspacePlaceholder
      title="Brugerstyring"
      description="Roller og invites hægtes på `User` + kommende RBAC — ikke en del af mock-listen endnu."
    />
  );
}
