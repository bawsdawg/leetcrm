/**
 * transactional email — Resend SDK, React Email templates in components/emails/.
 * @param {{ to: string; subject: string; html: string }} _params
 */
export async function sendEmail(_params) {
  throw new Error("Email not wired — add RESEND_API_KEY and implement send.");
}
