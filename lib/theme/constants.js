/** Persisted appearance — synced with `<html data-theme>` in `theme-init-script`. */

export const THEME_STORAGE_KEY = "apex-theme";

/** @typedef {"dark" | "light"} ThemeId */
export const THEMES = /** @type {const} */ (["dark", "light"]);
