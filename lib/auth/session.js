/**
 * Session helpers — wire to your auth library (Better Auth, Auth.js, Clerk, etc.).
 * @returns {Promise<{ user: { id: string } } | null>}
 */
export async function getSession() {
  return null;
}
